import React from 'react';
import ReactDOM from 'react-dom/client';
import FileManager from '../js/FileManager2/FileManager';

const root = ReactDOM.createRoot(document.getElementById('file-manager-root'));
root.render(<FileManager />);
